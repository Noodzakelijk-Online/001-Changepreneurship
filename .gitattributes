# Enforce Unix line endings for shell scripts (critical for Docker containers)
*.sh text eol=lf
entrypoint.sh text eol=lf

# Python files
*.py text eol=lf

# Keep Windows line endings for batch files
*.bat text eol=crlf
*.ps1 text eol=crlf

# Default behavior - let Git decide
* text=auto
